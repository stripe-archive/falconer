syntax = "proto3";
package jacquard;

import "github.com/stripe/veneur/ssf/sample.proto";

service Jacquard {

  rpc BidiSpans(stream ssf.SSFSpan) returns (SpanResponse) {}
}

message SpanResponse {
  string greeting = 1;
}
